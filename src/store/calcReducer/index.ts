import {
  addElement,
  removeElement,
  chooseOperation,
  clearAll,
  clearHistory,
  makeCalculations,
} from './calcReducer';

export const calcActions = {
  addElement,
  removeElement,
  chooseOperation,
  clearAll,
  clearHistory,
  makeCalculations,
};

export { calcReducer } from './calcReducer';
